import React from 'react';
import {
    AccessAlarm, Android, Watch, Headset, Flight, Mood, Star, Train, Tram, Traffic, DirectionsBike, Speaker, Cloud, ThumbUp, Rowing, Pets, PanTool, Face,
    DonutSmall, Home, Https, Settings, TouchApp, Update, AttachFile, FlashOn, WbSunny, WbIncandescent, LocalGasStation
} from '@material-ui/icons'
import styles from './styles';

const shapes = [
    <AccessAlarm className={JSON.stringify(styles.icon)} color="primary" />,
    <Android className={JSON.stringify(styles.icon)} color="primary" />,
    <Watch className={JSON.stringify(styles.icon)} color="primary" />,
    <Headset className={JSON.stringify(styles.icon)} color="primary" />,
    <Flight className={JSON.stringify(styles.icon)} color="primary" />,
    <Mood className={JSON.stringify(styles.icon)} color="primary" />,
    <Star className={JSON.stringify(styles.icon)} color="primary" />,
    <Train className={JSON.stringify(styles.icon)} color="primary" />,
    <Tram className={JSON.stringify(styles.icon)} color="primary" />,
    <Traffic className={JSON.stringify(styles.icon)} color="primary" />,
    <DirectionsBike className={JSON.stringify(styles.icon)} color="primary" />,
    <Speaker className={JSON.stringify(styles.icon)} color="primary" />,
    <Cloud className={JSON.stringify(styles.icon)} color="primary" />,
    <ThumbUp className={JSON.stringify(styles.icon)} color="primary" />,
    <Rowing className={JSON.stringify(styles.icon)} color="primary" />,
    <Pets className={JSON.stringify(styles.icon)} color="primary" />,
    <PanTool className={JSON.stringify(styles.icon)} color="primary" />,
    <Face className={JSON.stringify(styles.icon)} color="primary" />,
    <DonutSmall className={JSON.stringify(styles.icon)} color="primary" />,
    <Home className={JSON.stringify(styles.icon)} color="primary" />,
    <Https className={JSON.stringify(styles.icon)} color="primary" />,
    <Settings className={JSON.stringify(styles.icon)} color="primary" />,
    <TouchApp className={JSON.stringify(styles.icon)} color="primary" />,
    <Update className={JSON.stringify(styles.icon)} color="primary" />,
    <AttachFile className={JSON.stringify(styles.icon)} color="primary" />,
    <FlashOn className={JSON.stringify(styles.icon)} color="primary" />,
    <Tram className={JSON.stringify(styles.icon)} color="primary" />,
    <WbSunny className={JSON.stringify(styles.icon)} color="primary" />,
    <WbIncandescent className={JSON.stringify(styles.icon)} color="primary" />,
    <LocalGasStation className={JSON.stringify(styles.icon)} color="primary" />
]

export default shapes