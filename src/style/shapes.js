import React from 'react';
import {
    AccessAlarm, Android, Watch, Headset, Flight, Mood, Star, Train, Tram, Traffic, DirectionsBike, Speaker, Cloud, ThumbUp, Rowing, Pets, PanTool, Face,
    DonutSmall, Home, Https, Settings, TouchApp, Update, AttachFile, FlashOn, WbSunny, WbIncandescent, LocalGasStation
} from '@material-ui/icons'
import styles from '.';

const shapes = [
    <AccessAlarm className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Android className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Watch className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Headset className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Flight className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Mood className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Star className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Train className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Tram className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Traffic className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <DirectionsBike className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Speaker className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Cloud className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <ThumbUp className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Rowing className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Pets className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <PanTool className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Face className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <DonutSmall className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Home className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Https className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Settings className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <TouchApp className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Update className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <AttachFile className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <FlashOn className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <Tram className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <WbSunny className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <WbIncandescent className={JSON.stringify(styles.icon)} color="primary" size="large"/>,
    <LocalGasStation className={JSON.stringify(styles.icon)} color="primary" size="large"/>
]

export default shapes